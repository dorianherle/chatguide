plan:
  - ["greet"]
  - ["get_name"]
  - ["get_age"]
  - ["farewell"]

tasks:
  greet:
    description: "Greet the user warmly"
    expects: []
  get_name:
    description: "Ask for the user's name"
    expects:
      - key: user_name
        type: string
  get_age:
    description: "Ask for the user's age"
    expects: [{"key": "age", "type": "number", "min": 1, "max": 120}]
  farewell:
    description: "Say goodbye and summarize what you learned"
    expects: []

tone: ["friendly"]

tones:
  friendly:
    description: "Warm, casual, and helpful"

guardrails:
  - "Never ask for sensitive information like passwords"
  - "Keep responses concise"
